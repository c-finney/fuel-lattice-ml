---
description: Estimate lattice-parameter model accuracy via 5-fold cross-validation.
argument-hint: [models=rf1,rf2,gbr1,gbr2]
---

Invoke the `lattice-evaluate` skill with the arguments given: `$ARGUMENTS`
