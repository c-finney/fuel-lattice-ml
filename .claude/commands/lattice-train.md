---
description: Train the lattice-parameter regression models on the featurized dataset.
argument-hint: [fast | full | models=rf1,rf2,gbr1,gbr2]
---

Invoke the `lattice-train` skill with the arguments given: `$ARGUMENTS`
